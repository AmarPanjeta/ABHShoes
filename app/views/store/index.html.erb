<div class="row">
  <div class="col-md-8">
    <h1><%= flash[:item] %></h1>
    <h1><%= flash[:error] %></h1>
<div class="container">
  <div class="row">
    <% @shoes.each do |shoe| %>

    <div class="col-lg-3 col-md-4 col-xs-6 thumb shoe-box">
        <a href="<%=shoe_path(shoe.id)%>" class="thumbnail">
            <img class="img-responsive" src="<%=shoe.imgurl%>" style="height: 100px">
        </a>
        <p><%=shoe.name%></p>
        <%= button_to "Dodaj u korpu", "#", class: "btn add-to-cart", id:"shoe_#{shoe.id}" %>
    </div>
    <% end %>
  </div>
  <form action="/payments" method="POST">
  <script
    src="https://checkout.stripe.com/checkout.js"
    class="stripe-button"
    data-key="pk_test_6pRNASCoBOKtIshFeQd4XMUh"
    data-image="https://pbs.twimg.com/profile_images/570193155421503488/brZaisEE_reasonably_small.png"
    data-name="Demo Site"
    data-description="2 widgets ($20.00)"
    data-amount="2000">
  </script>
</form>
</div>

<div>
<h1>Store#index</h1>
<h2>Ovdje treba prikazivati artikle, trenutno nemam bazu</h2>
<h3>Neka raspored bude da lici na SPA </h3>
</div>
<%= link_to "Nabacani bootstrap elementi",store_dummy_path %>
</div>
</div>

<script>
$(document).ready(function(){
  $('.add-to-cart').click(function(e){
    e.preventDefault();
    console.log($(this).attr("id"));
    var id=$(this).attr("id").split("_")[1];
    var signed_in="<%=signed_in?%>";
    console.log(signed_in);



    toastr.options.positionClass = 'toast-bottom-full-width';
    toastr.options.extendedTimeOut = 0; //1000;
    toastr.options.timeOut = 1000;
    toastr.options.fadeOut = 250;
    toastr.options.fadeIn = 250;

    if(signed_in=="false") toastr.warning('Niste logovani')
    else {
    $.post("/line_items?shoe_id="+id,function(data){

      toastr.success('Uspjesno ste dodali artikal u korpu!')
      var cart=$("#cart-badge").html();
      $("#cart-badge").html(parseInt(cart)+1);

    })}


  })
})
</script>
